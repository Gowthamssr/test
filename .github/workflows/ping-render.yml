name: Keep Render App Awake

on:
  schedule:
    - cron: "*/14 * * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      # - name: Ping Render App
        # run: curl -s https://portfolio-backend-lsno.onrender.com
      - name: Ping Render App (second url)
        run: curl -s https://gowthamssr.tech
